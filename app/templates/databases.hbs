<div class='container push-down'>
  <h1>Databases</h1>

  <table class='table'>
    <thead>
      <tr>
        <th>Name</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      {{#each model as |databases|}}
        <tr>
          <td>
            {{database.name}}
          </td>
          <td>
            {{#link-to 'databases.database.tables' table.id class='btn btn-primary'}}
              Tables
            {{/link-to}}
          </td>
        </tr>
      {{/each}}
    </tbody>
  </table>
</div>
